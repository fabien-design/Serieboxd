package com.example.serieboxd.ui.components.card

enum class AppSerieCardVariant {
    DEFAULT,      // poster + titre + genre + année
    IN_PROGRESS,  // poster + titre + barre progression + S2·E6/10
    WATCHLIST,    // poster + titre + bouton "Commencer"
}
